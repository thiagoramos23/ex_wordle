# flyctl launch added from .elixir_ls/.gitignore
.elixir_ls/**/*

# flyctl launch added from .gitignore
# The directory Mix will write compiled artifacts to.
_build

# If you run "mix test --cover", coverage assets end up here.
cover

# The directory Mix downloads your dependencies sources to.
deps

# Where 3rd-party dependencies like ExDoc output generated docs.
doc

# Ignore .fetch files in case you like to edit your project deps locally.
.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
**/erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
**/*.ez

# Temporary files, for example, from tests.
tmp

# Ignore package tarball (built via "mix hex.build").
**/ex_wordle-*.tar

# Ignore assets that are produced by build tools.
priv/static/assets

# Ignore digested assets cache.
priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
**/npm-debug.log
assets/node_modules


# flyctl launch added from deps/live_motion/.elixir_ls/.gitignore
deps/live_motion/.elixir_ls/**/*

# flyctl launch added from deps/live_motion/.gitignore
# The directory Mix will write compiled artifacts to.
deps/live_motion/_build

# If you run "mix test --cover", coverage assets end up here.
deps/live_motion/cover

# The directory Mix downloads your dependencies sources to.
deps/live_motion/deps

# Where third-party dependencies like ExDoc output generated docs.
deps/live_motion/doc

# Ignore .fetch files in case you like to edit your project deps locally.
deps/live_motion/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
deps/live_motion/**/erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
deps/live_motion/**/*.ez

# Ignore package tarball (built via "mix hex.build").
deps/live_motion/**/live_motion-*.tar

# Temporary files, for example, from tests.
deps/live_motion/tmp

deps/live_motion/**/.elixir_ls

deps/live_motion/**/node_modules

deps/live_motion/**/.DS_Store
fly.toml
